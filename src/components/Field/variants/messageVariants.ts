import { tokens } from '@/tokens';

const { colors } = tokens;

export const messageVariants = {
  default: { color: colors.brand.secondary[900] },
  error: {
    color: colors.feedback.error.dark,
  },
};
